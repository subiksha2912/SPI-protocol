# SPI-protocol
This repository contains a SystemVerilog implementation of an SPI (Serial Peripheral Interface) Master along with a testbench to simulate SPI communication. The design supports sending commands, addresses, and data over the SPI bus in MSB-first format.
